<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rest-O-Rant" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Rest O Rant">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1 class="text-3xl font-bold text-center mt-8">Welcome to Rest-O-Rant</h1>
    <h2 class="text-xl text-center mt-2">A place to rest, enjoy your food, and rant your problems away!</h2>
    <button id="open-auth-modal"
            class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded mt-4">
      Get Started
    </button>

    <div id="auth-modal"
        class="fixed inset-0 hidden items-center justify-center z-50">

        <div class="fixed inset-0 bg-black opacity-50"></div>

        <div class="bg-white rounded-lg p-6 w-full max-w-md relative shadow-lg">
            <button id="close-auth-modal"
                    class="absolute top-2 right-3 text-gray-500 text-xl font-bold hover:text-red-400 cursor-pointer">
            &times;
            </button>

            <h2 class="text-2xl font-bold mb-4 text-center">Welcome to Shapi</h2>

            <div class="flex justify-center mb-4 space-x-4">
            <button id="login-tab" class="font-bold border-b-2 border-blue-500">Login</button>
            <button id="signup-tab" class="text-gray-500">Sign Up</button>
            </div>

            <div id="login-form">

            <%= form_with url: session_path, method: :post, data: { turbo: false } do |f| %>

                <% if flash.now[:login_errors] %>
                <div class="bg-red-100 text-red-500 border border-red-300 px-3 py-2 rounded mb-3 text-sm">
                    <%= flash.now[:login_errors] %>
                </div>

                <script>
                    document.addEventListener("DOMContentLoaded", function() {
                    const authModal = document.getElementById("auth-modal");
                    authModal.classList.remove("hidden");
                    authModal.classList.add("flex");
                    });
                </script>
                <% end %>

                <div class="mb-3">
                <%= f.label :email_address, "Email", class: "block" %>
                <%= f.email_field :email_address, placeholder: "Enter your email", class: "w-full border rounded px-2 py-1" %>
                </div>

                <div class="mb-3">
                <%= f.label :password, class: "block" %>
                <%= f.password_field :password, placeholder: "Enter your password", class: "w-full border rounded px-2 py-1" %>
                </div>

                <%= f.submit "Log In",
                    class: "w-full bg-blue-500 hover:bg-blue-600 text-white py-2 rounded" %>
            <% end %>

            </div>

            <div id="signup-form" class="hidden">

            <%= form_with model: User.new, url: users_path, method: :post, data: { turbo: false } do |f| %>

                <% if flash.now[:signup_errors] %>
                <div class="bg-red-100 text-red-500 border border-red-300 px-3 py-2 rounded mb-3 text-sm">
                    <%= flash.now[:signup_errors] %>
                </div>

                <script>
                    document.addEventListener("DOMContentLoaded", function() {
                    const authModal = document.getElementById("auth-modal");
                    authModal.classList.remove("hidden");
                    authModal.classList.add("flex");

                    loginTab = document.getElementById("login-tab");
                    signupTab = document.getElementById("signup-tab");
                    signupTab.classList.add("border-b-2", "border-blue-500", "font-bold");
                    loginTab.classList.remove("border-b-2", "border-blue-500", "font-bold");

                    const loginForm = document.getElementById("login-form");
                    const signupForm = document.getElementById("signup-form");
                    loginForm.classList.add("hidden");
                    signupForm.classList.remove("hidden");
                    });
                </script>
                <% end %>

                <div class="mb-3">
                <%= f.label :email_address, class: "block" %>
                <%= f.email_field :email_address, placeholder: "Enter your email", class: "w-full border rounded px-2 py-1" %>
                </div>

                <div class="mb-3">
                <%= f.label :password, class: "block" %>
                <%= f.password_field :password, placeholder: "Enter your password", class: "w-full border rounded px-2 py-1" %>
                </div>

                <div class="mb-3">
                <%= f.label :password_confirmation, class: "block" %>
                <%= f.password_field :password_confirmation, placeholder: "Confirm your password", class: "w-full border rounded px-2 py-1" %>
                </div>

                <%= f.submit "Create Account",
                    class: "w-full bg-blue-500 hover:bg-blue-600 text-white py-2 rounded" %>
            <% end %>

            </div>
        </div>
    </div>
  </body>
</html>
